# Repository Coordination

このリポジトリは、12 Week Year全体のビジョン・戦略・優先順位を管理する。

## 分野別の管理場所

- 英語：[`English/`](English/)
  - 英語学習の目標、具体的なアクション、教材、測定結果、進捗を管理する。
- 心と身体の健康：[`Health/`](Health/)
  - 睡眠、休息、運動、食事、美容、心身の状態と具体的なアクションを管理する。
- 仕事：[`Work/`](Work/)
  - Ampliumの開発、AI研究、仕事上の計画、実行記録、進捗を管理する。
- 人間性：[`Humanity/`](Humanity/)
  - 人間性に関する目標、実践、気づき、振り返りを管理する。

## 管理ルール

- このリポジトリでは、英語と健康を含む全分野のビジョン、12週間の戦略、優先順位、マイルストーンを管理する。
- 各分野の詳細な計画・実行記録・教材・測定結果は、対応する `English/`、`Health/`、`Work/`、`Humanity/` を正本として扱う。
- 分野別ディレクトリを変更するときは、そのディレクトリの `AGENTS.md` または `CLAUDE.md` を先に確認する。
- 各分野の重要な目標や方針が変わったときは、必要に応じてルートのREADMEや計画にも反映する。
- 同じ詳細情報を複数の場所へ重複して持たず、ルートには全体判断に必要な要約を残す。
- READMEの `## 今週のアクション` セクションは、チェックリスト同期（sync-worker）がチェック更新のたびに見出しと表だけで丸ごと再生成する。このセクションに表以外の内容を書いてはいけない。結果指標・方針・スケジュールなど今週のプランは、直前の `## 今週のプラン` セクションに書く。

## INSIGHTとnotesの書式

- 各分野の `INSIGHT.md` と `notes/` のファイルには、ファイル名と重複する先頭のタイトル見出しを付けない。最上位のセクション見出しを `#` から始める。
- `INSIGHT.md` には日付や出典を書かない。いつ・どこから来た内容かはgitの履歴で追う。

## Weekly Actionの達成判定

- 各アクションは、実行回数が週の目標回数の85%以上なら達成として扱う。
- 必要実行回数は `ceil(目標回数 × 0.85)` で算出する。たとえば目標7回なら6回で達成となる。
- この判定ルールは管理用の指示であり、READMEには記載しない。

## Gitの運用

- このリポジトリで変更をコミットしたら、必ずmainブランチに反映してプッシュする。作業ブランチに置いたままにしない。
- リモートセッション（claude.ai/code）では、コミット後にmainへの反映（マージとプッシュ）まで行うことをユーザーは常に許可している。作業ブランチへのプッシュ後、そのままmainへマージ・プッシュしてよい。

## AIとの会話記録

- 1つの分野に関するユーザーとAIの会話は、対応する `English/details/`、`Health/details/`、`Work/details/`、`Humanity/details/` にMarkdownで記録する。
- 一般的な会話、12 Week Year全体の相談、複数分野にまたがる会話は `conversations/` にMarkdownで記録する。
- 複数分野にまたがる会話の一部を分野別資料として残す必要がある場合は、会話全体を `conversations/` に置き、その分野の要点だけを対応する `details/` に残す。
- ファイル名はバンクーバー時間の日付を使い、`yyyy-mm-dd-<topic>.md` とする。`<topic>` は内容が分かる短い英語のkebab-caseにする。
- 同じ日・同じトピックの会話は既存ファイルへ追記し、別トピックなら新しいファイルを作る。
- 計画、判断、方針変更、振り返り、重要な質疑応答を随時記録する。単純な実行確認やツール出力は省略してよい。
- 各発言は時系列で `## User` と `## AI` の見出しを使って記録する。長い会話は意味を変えない範囲で簡潔にまとめてもよい。
- パスワード、APIトークン、Cookieなどの秘密情報は記録しない。

## Codex の呼び出し方

Mac Studio 上の codex-server（`codex exec` の HTTP ラッパー）を経由して Codex に相談できる。

- ユーザーが「Codex に壁打ち・相談して」と言ったら呼び出す。デフォルトは `effort: "medium"` + `fast: true`。ユーザーが指定したら `xhigh` などに変更する
- 接続情報は環境変数 `CODEX_WEB_SERVER_URL` と `CODEX_WEB_SERVER_SECRECT_KEY` にある（URLはトンネル再起動で変わることがある）

```bash
curl -sS -X POST "$CODEX_WEB_SERVER_URL/run" \
  -H "Authorization: Bearer $CODEX_WEB_SERVER_SECRECT_KEY" \
  -H 'Content-Type: application/json' \
  -d '{"prompt":"<相談内容>","effort":"medium","fast":true}'
```

- レスポンスの `sessionId` を次のリクエストに `"sessionId": "<id>"` として渡すと同じセッションで会話を継続できる
- `GET /health`（認証不要）で疎通確認できる
