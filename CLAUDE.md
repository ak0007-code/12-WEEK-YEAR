# Repository Coordination

このリポジトリは、12 Week Year全体のビジョン・戦略・優先順位を管理する。

## 分野別の管理場所

- 英語：[`English/`](English/)
  - 英語学習の目標、具体的なアクション、教材、測定結果、進捗を管理する。
- 心と身体の健康：[ak0007-code/Health](https://github.com/ak0007-code/Health)
  - 睡眠、休息、運動、食事、美容、心身の状態と具体的なアクションを管理する。

## 管理ルール

- このリポジトリでは、英語と健康を含む全分野のビジョン、12週間の戦略、優先順位、マイルストーンを管理する。
- 英語の詳細な計画・実行記録・教材・測定結果は `English/` を正本として扱う。旧 `ak0007-code/English` リポジトリは正本として更新しない。
- `English/` を変更するときは、同ディレクトリの `AGENTS.md` または `CLAUDE.md` を先に確認する。
- 健康の詳細な計画・実行記録・測定結果は、引き続き連携リポジトリを正本として扱う。
- 英語または健康の重要な目標や方針が変わったときは、必要に応じてルートのREADMEや計画にも反映する。
- 同じ詳細情報を複数のリポジトリへ重複して持たず、このリポジトリには全体判断に必要な要約を残す。
- 健康の連携リポジトリを変更する場合は、そのリポジトリにある指示ファイルを先に確認する。

## Weekly Actionの達成判定

- 各アクションは、実行回数が週の目標回数の85%以上なら達成として扱う。
- 必要実行回数は `ceil(目標回数 × 0.85)` で算出する。たとえば目標7回なら6回で達成となる。
- この判定ルールは管理用の指示であり、READMEには記載しない。

## Gitの運用

- このリポジトリで変更をコミットしたら、必ずmainブランチに反映してプッシュする。作業ブランチに置いたままにしない。

## AIとの会話記録

- ユーザーとAIの会話は `conversations/` にMarkdownで記録する。
- ファイル名はバンクーバー時間の日付を使い、`yyyy-mm-dd-<topic>.md` とする。`<topic>` は内容が分かる短い英語のkebab-caseにする。
- 同じ日・同じトピックの会話は既存ファイルへ追記し、別トピックなら新しいファイルを作る。
- 計画、判断、方針変更、振り返り、重要な質疑応答を随時記録する。単純な実行確認やツール出力は省略してよい。
- 各発言は時系列で `## User` と `## AI` の見出しを使って記録する。長い会話は意味を変えない範囲で簡潔にまとめてもよい。
- パスワード、APIトークン、Cookieなどの秘密情報は記録しない。
